include "envs/gathering_replication_env.gin"

train_eval.env_id = "cum-rewards-env"           # Used to name the folder for logs/summaries/checkpoints

GatheringWrapper.cumulative_rewards_flag = True
