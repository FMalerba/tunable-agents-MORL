include "envs/replication_env.gin"

train_eval.env_id = "cum_target_env"           # Used to name the folder for logs/summaries/checkpoints

GatheringWrapper.cumulative_rewards_flag = True
GatheringWrapper.utility_type = 'target'
