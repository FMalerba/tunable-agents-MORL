include "envs/replication_env.gin"

train_eval.env_id = "linear_dual_threshold_env"           # Used to name the folder for logs/summaries/checkpoints

GatheringWrapper.utility_type = 'linear_dual_threshold'
