include "envs/linear_env.gin"

train_eval.env_id = "cum_linear_threshold_env"           # Used to name the folder for logs/summaries/checkpoints

GatheringWrapper.cumulative_rewards_flag = True
GatheringWrapper.utility_type = 'linear_threshold'
